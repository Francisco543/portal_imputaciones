$(document).ready(function () {
    // Obtén el ID del contacto autenticado
    var contactoId = document.getElementById("contactId").value;
    console.log(contactoId);
    // Si necesitas más información, podrías llamar a una API personalizada
});